import 'package:flutter/material.dart';

class Pokeball {
  String name;
  List<Color> colors;
  String asset;
  int pokemons;
  int cost;

  Pokeball({this.name, this.colors, this.asset, this.pokemons, this.cost});
}
